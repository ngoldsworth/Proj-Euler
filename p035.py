from primes.primelist import PrimesList, primesfrom2to

def circle_number(num: int):
    digilist = list(str(num))
    circled = [num]
    for j in range(len(digilist)):




max_num = 10**6 + 1
plist = PrimesList(max_num)
