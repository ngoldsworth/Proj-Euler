import numpy as np

def solve_tree(tree: np.ndarray):
    sums_arr =        axs.append(subax)
        figs.append(subfig)


    figs, axs = plt.subplots(1, 2)
    axs[0] = subax[0]
    axs[1] = subax[1]
    plt.show()
